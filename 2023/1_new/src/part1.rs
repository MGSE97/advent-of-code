use colored::Colorize;

pub async fn solve() {
    // Code here
    todo!();

    print!(
        "{prefix}: {question}",
        prefix = "Question".blue(),
        question = "".yellow().bold()
    );
    print!(
        "{prefix}: {answer}",
        prefix = "  Answer".blue(),
        answer = "".green().bold()
    );
}
