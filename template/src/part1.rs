use colored::Colorize;

pub async fn solve() -> Result<(), String> {
    // Code here
    todo!();

    println!(
        "{prefix}: {question}",
        prefix = "Question".blue(),
        question = "".yellow().bold()
    );
    println!(
        "{prefix}: {answer}",
        prefix = "  Answer".blue(),
        answer = "".green().bold()
    );
    Ok(())
}
