use colored::Colorize;

pub async fn solve() {
    print!(
        "{prefix}: {question}",
        prefix = "Question".blue(),
        question = "".yellow().bold()
    );

    // Code here
    todo!();

    print!(
        "{prefix}: {answer}",
        prefix = "Answer".blue(),
        answer = "".green().bold()
    );
}
